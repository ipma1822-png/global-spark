// GLOBAL SPARK PHASE 3-3 · v3.3.0
(function(){
const $=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));
const center=new URLSearchParams(location.search).get('center');let members=[],rules=[],member=null,rule=null,growth=[];const pLabels={solo:'혼자',friends:'친구와',family:'가족과',center:'센터와',community:'지역사회와'},flameLabels={GOOD:'착한',SAFE:'안전',EARTH:'환경',CHALLENGE:'도전',CITIZEN:'함께'};
if(!center){$('status').textContent='MY CENTER에서 센터를 선택해 주세요.';return}$('centerTitle').textContent=center+' · SPARK 입력';
function drawMembers(){$('memberGrid').innerHTML=members.filter(x=>x.active).map(x=>`<div class="member-card"><button class="member ${member===x.id?'active':''}" data-id="${x.id}"><b>${esc(x.display_name)}</b><small>${esc(x.member_code||'')}</small></button><div class="member-tools"><button class="member-room" data-room-id="${x.id}" data-room-name="${esc(x.display_name)}">🔥 내 SPARK</button><button class="member-link" data-link-id="${x.id}" data-link-name="${esc(x.display_name)}">🔗 개인 링크</button><button class="member-revoke" data-revoke-id="${x.id}" data-revoke-name="${esc(x.display_name)}">⛔ 해제</button></div></div>`).join('')||'등록된 아이가 없습니다.'}
function drawRules(){$('ruleGrid').innerHTML=rules.map(x=>`<button class="rule ${rule===x.activity_type?'active':''}" data-type="${esc(x.activity_type)}"><b>${esc(x.label_ko)}</b><small>+${x.xp} XP</small></button>`).join('')}
function drawGrowth(){const q=($('growthSearch').value||'').trim().toLowerCase(),f=$('growthFilter').value;const today=new Date().toDateString();const rows=growth.filter(x=>{if(q&&!String(x.display_name).toLowerCase().includes(q))return false;if(f==='attention'&&!x.needs_attention)return false;if(f==='inactive'&&x.last_activity&&Date.now()-new Date(x.last_activity).getTime()<7*86400000)return false;if(f==='today'&&(!x.last_activity||new Date(x.last_activity).toDateString()!==today))return false;return true});$('growthList').innerHTML=rows.length?rows.map(x=>`<div class="growth-row ${x.needs_attention?'attn':''}" data-growth-id="${x.id}"><div><b>${esc(x.display_name)}</b><small>${x.needs_attention?'⚠ 확인 필요':'✓ 정상 흐름'}</small></div><div><b>${x.total_xp} XP</b><small>누적</small></div><div><b>${x.activity_7d}회</b><small>최근 7일</small></div><div class="optional"><b>${esc(flameLabels[x.dominant_flame]||'-')}</b><small>주요 불꽃</small></div><div class="optional"><b>${x.repeat_7d>=5?'⚠ '+x.repeat_7d:x.repeat_7d}회</b><small>동일활동 최대</small></div></div>`).join(''):'조건에 맞는 회원이 없습니다.'}
async function loadGrowth(){try{const d=await SparkData.centerGrowthDashboard(center),s=d.summary||{};growth=d.members||[];$('dashStats').innerHTML=[['전체',s.active_members||0],['오늘 활동',s.active_today||0],['7일 활동',s.active_7d||0],['7일 미활동',s.inactive_7d||0],['7일 XP',s.xp_7d||0]].map(x=>`<div class="stat"><small>${x[0]}</small><b>${x[1]}</b></div>`).join('');drawGrowth()}catch(e){console.error(e);$('growthList').textContent='성장관리판을 불러오지 못했습니다. 로그인/권한을 확인해 주세요.'}}
async function recent(){try{const a=await SparkData.centerGetRecent(center,30);$('recentList').innerHTML=a.length?a.map(x=>`<div class="recent-item"><div><b>${esc(x.display_name)} · ${esc(x.label_ko)}</b><small>${new Date(x.created_at).toLocaleString('ko-KR')}</small></div><b>+${x.net_xp} XP</b></div>`).join(''):'최근 기록이 없습니다.'}catch(e){$('recentList').textContent='최근 기록을 불러오지 못했습니다.'}}
function missionCard(m,canConfirm){return `<article class="mission-card ${m.completed?'done':''}" data-id="${m.id}"><small>${esc(m.flame_code)} · ${esc(m.target_label||'모두')} · ${esc(pLabels[m.participation_type]||m.participation_type)}</small><h3>${m.completed?'✅ ':''}${esc(m.title)}</h3><p>${esc(m.description)}</p>${m.completed?'센터 확인 완료':canConfirm?'<button class="secondary mission-done">✅ 현실 행동 완료 확인</button>':'회원 선택 후 확인 가능'}</article>`}
async function missions(){try{const rows=member?await SparkData.memberMissions(member):await SparkData.publicMissions(20);$('missionList').innerHTML=rows.length?rows.map(m=>missionCard(m,!!member)).join(''):'현재 공개된 MISSION이 없습니다.'}catch(e){$('missionList').textContent='MISSION을 불러오지 못했습니다.'}}
async function load(){try{[members,rules]=await Promise.all([SparkData.centerGetMembers(center),SparkData.centerGetRules()]);const first=members.find(x=>x.active);if(first)member=first.id;drawMembers();drawRules();recent();missions();loadGrowth()}catch(e){console.error(e);$('status').textContent='센터 지도자 로그인과 권한을 확인해 주세요.'}}
function kidUrl(token){const u=new URL('spark-room.html',location.href);u.search='';u.hash='';u.searchParams.set('kid',token);return u.href}async function copyText(text){try{if(navigator.clipboard&&window.isSecureContext){await navigator.clipboard.writeText(text);return true}const ta=document.createElement('textarea');ta.value=text;document.body.appendChild(ta);ta.select();const ok=document.execCommand('copy');ta.remove();return ok}catch(_){return false}}
$('memberGrid').onclick=async e=>{const link=e.target.closest('.member-link');if(link){const id=link.dataset.linkId,name=link.dataset.linkName||'아이';if(!confirm(`${name}의 개인 SPARK 링크를 새로 발급할까요?`))return;link.disabled=true;try{const r=await SparkData.rpc('spark_create_kid_link',{p_member_id:id}),row=Array.isArray(r)?r[0]:r,url=kidUrl(row?.token);if(!row?.token)throw new Error('TOKEN');const copied=await copyText(url);$('status').textContent=`🔗 ${name} 개인 링크 발급${copied?'·복사 완료':''}`;if(!copied)prompt(`${name} 개인 SPARK 링크`,url)}catch(err){$('status').textContent='개인 링크 발급에 실패했습니다.'}finally{link.disabled=false}return}const revoke=e.target.closest('.member-revoke');if(revoke){const id=revoke.dataset.revokeId,name=revoke.dataset.revokeName||'아이';if(!confirm(`${name}의 개인 SPARK 링크를 해제할까요?`))return;try{await SparkData.rpc('spark_revoke_kid_link',{p_member_id:id});$('status').textContent=`⛔ ${name} 개인 링크 해제 완료`}catch(err){$('status').textContent='개인 링크 해제에 실패했습니다.'}return}const room=e.target.closest('.member-room');if(room){window.open(`spark-room.html?center=${encodeURIComponent(center)}&member=${encodeURIComponent(room.dataset.roomId)}&from=center`,'_blank','noopener');return}const b=e.target.closest('.member');if(b){member=b.dataset.id;drawMembers();missions()}};
$('growthList').onclick=e=>{const r=e.target.closest('[data-growth-id]');if(r){member=r.dataset.growthId;drawMembers();missions();document.getElementById('memberGrid').scrollIntoView({behavior:'smooth',block:'center'})}};$('growthSearch').oninput=drawGrowth;$('growthFilter').onchange=drawGrowth;$('refreshGrowth').onclick=loadGrowth;
$('ruleGrid').onclick=e=>{const b=e.target.closest('.rule');if(b){rule=b.dataset.type;drawRules()}};$('missionList').onclick=async e=>{const b=e.target.closest('.mission-done');if(!b||!member)return;try{await SparkData.confirmMission(member,b.closest('.mission-card').dataset.id);await missions();await loadGrowth()}catch(err){$('status').textContent='MISSION 완료 확인 실패'}};function selectedName(){return members.find(x=>x.id===member)?.display_name||''}function pop(xp){$('successName').textContent=selectedName();$('successXp').textContent='+'+xp+' XP';$('successPop').classList.add('show');setTimeout(()=>$('successPop').classList.remove('show'),1100)}
$('registerBtn').onclick=async()=>{if(!member||!rule){$('status').textContent='아이와 좋은 행동을 선택해 주세요.';return}try{const r=await SparkData.centerRegisterActivity(center,member,rule,$('activityMemo').value.trim());$('status').textContent=`🔥 ${selectedName()} +${r.xp} XP · 등록 완료`;pop(r.xp);$('activityMemo').value='';await Promise.all([recent(),loadGrowth()])}catch(e){$('status').textContent='등록에 실패했습니다.'}};$('undoBtn').onclick=async()=>{if(!confirm('선택한 아이의 최근 SPARK 기록을 되돌릴까요?'))return;try{await SparkData.centerUndoLast(center,member);$('status').textContent='UNDO 완료';await Promise.all([recent(),loadGrowth()])}catch(e){$('status').textContent='되돌릴 기록이 없습니다.'}};
$('voiceBtn').onclick=()=>{const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR){$('status').textContent='이 브라우저는 음성 인식을 지원하지 않습니다.';return}const sr=new SR();sr.lang='ko-KR';sr.onresult=e=>{const t=e.results[0][0].transcript.replace(/\s/g,''),m=members.find(x=>t.includes(String(x.display_name).replace(/\s/g,''))),rr=rules.find(x=>t.includes(String(x.label_ko).replace(/[·\s]/g,'')));if(m)member=m.id;if(rr)rule=rr.activity_type;drawMembers();drawRules();if(m)missions();$('status').textContent=m&&rr?`🎙 ${m.display_name} · ${rr.label_ko} 선택 완료`:'아이 또는 행동을 찾지 못했습니다.'};sr.start()};load();
})();