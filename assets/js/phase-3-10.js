// GLOBAL SPARK PHASE 3-10 · v3.10.1
(function(){
const page=location.pathname.split('/').pop(),$=id=>document.getElementById(id),sleep=ms=>new Promise(r=>setTimeout(r,ms));
function toast(t){const el=$('toast');if(el){el.textContent=t;el.hidden=false;setTimeout(()=>el.hidden=true,2600)}else alert(t)}
function version(){document.title='MY SPARK · PHASE 3-10 · v3.10.1';const pills=[...document.querySelectorAll('.version-pill,.ver')];pills.forEach((x,i)=>{x.textContent=i===0?'PHASE 3-10 · v3.10.1':'';if(i>0)x.style.display='none'})}
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function ensureCampaignSection(){let list=$('campaignList');if(list)return list;const mission=$('missionList')?.closest('section')||$('missionList')?.parentElement;if(!mission)return null;const sec=document.createElement('section');sec.className='section';sec.id='campaignSection';sec.innerHTML='<div class="section-title-row"><div><p class="eyebrow">GLOBAL CAMPAIGN</p><h2>🌍 세계 캠페인</h2></div><span class="tiny-note">참여하고 실제 행동을 기록하세요</span></div><div id="campaignList" class="mission-list"><p class="empty">캠페인 불러오는 중…</p></div>';mission.parentNode.insertBefore(sec,mission);return $('campaignList')}
function campaignCard(c){const n=Number(c.action_count||0),earth=c.flame_code==='EARTH',flame=earth?'🌿 환경':'🔥 '+esc(c.flame_code),actionLabel=earth?'🌿 오늘 환경 행동 기록 +5 XP':'🔥 오늘 캠페인 행동 기록 +5 XP';return `<article class="mission-card ${c.joined?'done':''}" data-campaign-id="${c.id}"><div class="mission-meta">${flame} · ${new Date(c.starts_at).toLocaleDateString('ko-KR')} ~ ${c.ends_at?new Date(c.ends_at).toLocaleDateString('ko-KR'):'계속'}</div><h3>${c.joined?'✅ ':''}${esc(c.title)}</h3><p>${esc(c.description||'')}</p><div class="campaign-action-box" style="margin-top:12px;padding-top:12px;border-top:1px solid rgba(255,255,255,.10)"><small style="display:block;margin-bottom:8px;opacity:.75">실제 실천 ${n}회</small>${c.joined?`<button type="button" class="primary campaign-action-btn" data-id="${c.id}">${actionLabel}</button>`:`<button type="button" class="primary join-campaign" data-campaign-id="${c.id}">🌍 이 캠페인 참여하기</button>`}</div></article>`}
async function mySpark(){version();let busy=false;async function refresh(){const sel=$('memberSelect');if(!sel?.value||!window.SparkData?.isSignedIn())return;const list=ensureCampaignSection();if(!list)return;try{const rows=await SparkData.memberCampaigns(sel.value);list.innerHTML=rows.length?rows.map(campaignCard).join(''):'<p class="empty">현재 참여할 글로벌 캠페인이 없습니다.</p>'}catch(err){console.error(err);list.innerHTML='<p class="empty">캠페인을 불러오지 못했습니다.</p>'}}
const obs=new MutationObserver(()=>{if(!$('campaignList')||!$('campaignList').querySelector('.campaign-action-btn,.join-campaign'))refresh()});obs.observe(document.body,{childList:true,subtree:true});document.addEventListener('change',e=>{if(e.target?.id==='memberSelect')setTimeout(refresh,150)});document.addEventListener('click',async e=>{const action=e.target.closest('.campaign-action-btn');if(action&& !busy){const member=$('memberSelect')?.value;if(!member)return;const memo=prompt('오늘 어떤 행동을 했나요?\n예: 분리수거, 물 절약, 쓰레기 줍기, 전기 절약');if(memo===null)return;try{busy=true;action.disabled=true;const r=await SparkData.rpc('spark_complete_campaign_action',{p_campaign_id:action.dataset.id,p_member_id:member,p_memo:memo.trim()});toast(`🌿 행동 기록 완료 · +${r.xp||5} XP`);await refresh();setTimeout(()=>location.reload(),700)}catch(err){console.error(err);toast('캠페인 행동 기록에 실패했습니다.')}finally{busy=false;action.disabled=false}return}
const join=e.target.closest('.join-campaign');if(join&&join.dataset.campaignId){setTimeout(refresh,500)}});await sleep(350);ensureCampaignSection();refresh();setTimeout(refresh,1000)}
async function hq(){document.title='HQ COMMAND CENTER · PHASE 3-10 · v3.10.1';document.querySelectorAll('.ver').forEach(x=>x.textContent='PHASE 3-10 · v3.10.1');async function paint(){if(!window.SparkData?.isSignedIn())return;let stats;try{stats=await SparkData.hqCampaignStats()}catch(_){return}const rows=[...document.querySelectorAll('.campaign-card .row')];for(const s of stats){const row=rows.find(r=>r.textContent.includes(s.title));if(!row)continue;const span=row.querySelector('span');if(span&&!span.textContent.includes('실천'))span.textContent+=` · 실천 ${Number(s.actions||0)}회`}}const box=$('commandData');if(box)new MutationObserver(()=>paint()).observe(box,{childList:true,subtree:true});setTimeout(paint,700)}
async function live(){document.title='SPARK LIVE · PHASE 3-10 · v3.10.1';async function paint(){let stats;try{stats=await SparkData.publicCampaignStats()}catch(_){return}const cards=[...document.querySelectorAll('#campaignList .campaign-card')];for(const s of stats){const card=cards.find(c=>c.textContent.includes(s.title));if(!card)continue;const small=[...card.querySelectorAll('small')].find(x=>x.textContent.includes('명 참여'));if(small&&!small.textContent.includes('실천'))small.textContent+=` · 🌿 실천 ${Number(s.actions||0)}회`}}const box=$('campaignList');if(box)new MutationObserver(()=>paint()).observe(box,{childList:true,subtree:true});setTimeout(paint,700)}
if(page==='my-spark.html')mySpark();else if(page==='hq-command.html')hq();else if(page==='spark-live.html')live();
})();