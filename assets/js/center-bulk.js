// GLOBAL SPARK · GB-04 group activity bulk SPARK · v3.20.0
(function(){
  const $=id=>document.getElementById(id),center=new URLSearchParams(location.search).get('center'),selected=new Set();
  const grid=$('memberGrid'),countEl=$('bulkCount'),allBtn=$('bulkSelectAll'),clearBtn=$('bulkClear'),bulkBtn=$('bulkRegisterBtn'),status=$('status');
  if(!center||!grid||!countEl||!allBtn||!clearBtn||!bulkBtn)return;
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const toolbar=allBtn.parentElement;
  const search=document.createElement('input');search.type='search';search.placeholder='🔎 아이 이름·회원번호 검색';search.autocomplete='off';search.style.cssText='min-width:210px;flex:1 1 220px;background:#0d1526;color:#fff;border:1px solid rgba(255,255,255,.10);border-radius:12px;padding:10px 12px';
  const filter=document.createElement('select');filter.innerHTML='<option value="all">전체 아이</option><option value="today">오늘 활동한 아이</option><option value="active7">7일 내 활동한 아이</option><option value="inactive7">7일 미활동 아이</option><option value="attention">확인 필요한 아이</option><option value="nolink">개인링크 없는 아이</option>';filter.style.cssText='min-width:170px;background:#0d1526;color:#fff;border:1px solid rgba(255,255,255,.10);border-radius:12px;padding:10px 12px';
  toolbar.insertBefore(search,allBtn);toolbar.insertBefore(filter,allBtn);allBtn.textContent='☑ 보이는 아이 전체 선택';bulkBtn.textContent='🔥 선택한 아이들에게 단체 SPARK';
  const quick=document.createElement('div');quick.style.cssText='display:flex;gap:6px;flex-wrap:wrap;width:100%;margin-top:4px';quick.innerHTML='<button type="button" data-q="today">오늘 활동 전체</button><button type="button" data-q="attention">확인 필요 전체</button><button type="button" data-q="inactive7">7일 미활동 전체</button><button type="button" data-q="nolink">링크 없는 아이 전체</button>';[...quick.children].forEach(b=>b.style.cssText='padding:7px 10px;border-radius:999px;background:#172743;color:#fff;border:1px solid rgba(255,255,255,.12);cursor:pointer');toolbar.appendChild(quick);
  const picked=document.createElement('div');picked.style.cssText='display:none;width:100%;margin-top:4px;padding:10px 12px;border-radius:12px;background:#0d1526;border:1px solid rgba(255,159,28,.30);font-size:12px;line-height:1.8';toolbar.appendChild(picked);
  let stats=new Map();
  function cards(){return [...grid.querySelectorAll('.member-card')]}
  function nameFor(id){const b=grid.querySelector(`.member[data-id="${CSS.escape(id)}"]`);return b?.querySelector('b')?.textContent?.trim()||id}
  function drawPicked(){const ids=[...selected];if(!ids.length){picked.style.display='none';picked.textContent='';return}picked.style.display='block';picked.innerHTML='<b>단체활동 대상</b> · '+ids.map(id=>`<button type="button" data-remove-id="${esc(id)}" style="display:inline-block;margin:3px 4px 0 0;padding:3px 8px;border-radius:999px;background:#ff9f1c16;color:#fff;border:1px solid #ff9f1c44;cursor:pointer">${esc(nameFor(id))} ×</button>`).join('')}
  function matches(id,f=filter.value){const x=stats.get(id);if(f==='all')return true;if(!x)return false;if(f==='today')return !!x.last_activity&&new Date(x.last_activity).toDateString()===new Date().toDateString();if(f==='active7')return !!x.last_activity&&(Date.now()-new Date(x.last_activity).getTime()<7*86400000);if(f==='inactive7')return !x.last_activity||(Date.now()-new Date(x.last_activity).getTime()>=7*86400000);if(f==='attention')return !!x.needs_attention;if(f==='nolink')return !x.link_active;return true}
  function applyFilter(){const q=search.value.trim().toLowerCase();cards().forEach(card=>{const b=card.querySelector('.member[data-id]');if(!b)return;card.hidden=!((!q||(b.textContent||'').toLowerCase().includes(q))&&matches(b.dataset.id))});sync(false)}
  function visible(){return cards().filter(c=>!c.hidden).map(c=>c.querySelector('.member[data-id]')).filter(Boolean)}
  function sync(reapply=true){cards().forEach(card=>{const b=card.querySelector('.member[data-id]');if(!b)return;let cb=card.querySelector('.bulk-check');if(!cb){cb=document.createElement('input');cb.type='checkbox';cb.className='bulk-check';cb.dataset.bulkId=b.dataset.id;card.appendChild(cb)}cb.checked=selected.has(b.dataset.id);card.classList.toggle('bulk-picked',selected.has(b.dataset.id))});countEl.textContent=`${selected.size}명 선택 · ${visible().length}명 표시`;drawPicked();if(reapply)applyFilter()}
  async function loadStats(){try{const d=await SparkData.centerGrowthDashboard(center);stats=new Map((d.members||[]).map(x=>[x.id,x]));applyFilter()}catch(e){console.error(e);status.textContent='회원 상태 필터를 불러오지 못했습니다. 이름 검색과 개별 선택은 계속 사용할 수 있습니다.'}}
  search.oninput=applyFilter;filter.onchange=applyFilter;
  grid.addEventListener('change',e=>{const cb=e.target.closest('.bulk-check');if(!cb)return;cb.checked?selected.add(cb.dataset.bulkId):selected.delete(cb.dataset.bulkId);sync(false)});
  allBtn.onclick=()=>{visible().slice(0,100).forEach(b=>selected.add(b.dataset.id));sync(false)};clearBtn.onclick=()=>{selected.clear();sync(false)};
  quick.onclick=e=>{const b=e.target.closest('[data-q]');if(!b)return;const q=b.dataset.q,ids=cards().map(c=>c.querySelector('.member[data-id]')).filter(Boolean).filter(x=>matches(x.dataset.id,q)).slice(0,100);ids.forEach(x=>selected.add(x.dataset.id));sync(false);status.textContent=`${b.textContent} · ${ids.length}명을 선택했습니다. 필요한 아이는 이름표의 ×로 제외할 수 있습니다.`};
  picked.onclick=e=>{const b=e.target.closest('[data-remove-id]');if(!b)return;selected.delete(b.dataset.removeId);sync(false)};
  bulkBtn.onclick=async()=>{const ids=[...selected];if(!ids.length){status.textContent='단체 SPARK를 받을 아이를 체크해 주세요.';return}if(ids.length>100){status.textContent='한 번에 최대 100명까지 선택할 수 있습니다.';return}const rule=document.querySelector('#ruleGrid .rule.active');if(!rule){status.textContent='여러 아이에게 줄 좋은 행동을 먼저 선택해 주세요.';return}const type=rule.dataset.type,label=rule.querySelector('b')?.textContent||'선택한 활동',xpText=rule.querySelector('small')?.textContent||'',preview=ids.slice(0,12).map(nameFor).join(', ')+(ids.length>12?` 외 ${ids.length-12}명`:'');if(!confirm(`${ids.length}명의 단체활동으로 “${label}” ${xpText}를 한 번에 기록할까요?\n\n대상: ${preview}\n\n이 기록은 하나의 단체활동 묶음으로 보관됩니다.`))return;bulkBtn.disabled=true;bulkBtn.textContent='🔥 단체활동 등록 중…';try{const r=await SparkData.rpc('spark_center_bulk_register_activity',{p_center_code:center,p_member_ids:ids,p_activity_type:type,p_memo:$('activityMemo')?.value.trim()||''}),row=Array.isArray(r)?r[0]:r;status.textContent=`🔥 단체활동 완료 · ${Number(row?.member_count||ids.length)}명 · 1인 +${Number(row?.xp_each||0)} XP · 묶음 ${String(row?.batch_id||'').slice(0,8)}`;selected.clear();sync(false);if($('activityMemo'))$('activityMemo').value='';setTimeout(()=>location.reload(),900)}catch(e){const m=String(e?.message||e);status.textContent=m.includes('TOO_MANY_MEMBERS')?'한 번에 최대 100명까지 선택할 수 있습니다.':m.includes('BATCH_MEMBER_INVALID')?'선택한 아이 중 현재 성장기지에서 사용할 수 없는 회원이 있습니다.':'단체 SPARK 등록에 실패했습니다.'}finally{bulkBtn.disabled=false;bulkBtn.textContent='🔥 선택한 아이들에게 단체 SPARK'}};
  new MutationObserver(()=>{sync(false);applyFilter()}).observe(grid,{childList:true});sync(false);loadStats();
})();